# Copy this file to `.env` and put your real key there.
# `.env` is gitignored — it never gets committed or pushed.
# Vite injects VITE_-prefixed vars into the build; the AI test script reads it too.
VITE_ANTHROPIC_API_KEY=
